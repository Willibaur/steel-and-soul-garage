---
import InstagramIcon from '@icons/instagram.svg'

interface Props {
  className?: string
  color?: string
  size?: number
}

const { className, color = '#FFFFFF', size = 24 } = Astro.props
---

<InstagramIcon class={className} height={size} width={size} fill={color} />
