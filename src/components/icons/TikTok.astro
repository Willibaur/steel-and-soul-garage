---
import TikTokIcon from '@icons/tiktok.svg'

interface Props {
  className?: string
  color?: string
  size?: number
}

const { className, color = '#FFFFFF', size = 24 } = Astro.props
---

<TikTokIcon class={className} fill={color} height={size} width={size} />
