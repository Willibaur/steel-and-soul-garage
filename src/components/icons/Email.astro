---
import EmailIcon from '@icons/email.svg'

interface Props {
  className?: string
  color?: string
  size?: number
}

const { className, color = '#FFFFFF', size = 24 } = Astro.props
---

<EmailIcon class={className} height={size} width={size} />
