---
import PeopleIcon from '@icons/people.svg'

interface Props {
  className?: string
  color?: string
  size?: number
}

const { className, color = '#FFFFFF', size = 24 } = Astro.props
---

<PeopleIcon class={className} height={size} width={size} />
