---
import QrCodeIcon from '@icons/qr-code.svg'

interface Props {
  className?: string
  color?: string
  size?: number
}

const { className, color = '#FFFFFF', size = 24 } = Astro.props
---

<QrCodeIcon class={className} height={size} width={size} fill={color} />
